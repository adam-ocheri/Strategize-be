{ 
    "compilerOptions": 
    {
        "target" : "ESNext",				        // <= target ES version ("ESNext" is latest)
        "module": "ESNext",
        "lib": [ "dom", "es2017", "ESNext"],			// <= integrate intellisense, documentation and autocomplete features for libraries needed for a project
        "allowSyntheticDefaultImports":true,
        "moduleResolution": "node",
        "rootDir": "./",
        "outDir": "./build"
        
        //"start": "rimraf ./build && npx tsc && node build/index.js",
    }
 }